from functions import *

first_file = 'file.first'
second_file = 'file.second'
file_input(first_file)
find_faculty(input("Введіть назву міста в якому хочете знайти найбільший факультет: "), first_file)
create_second_file(first_file, second_file)
output_first_file(first_file)
output_second_file(second_file)